# Everyday Plan 源文件功能说明

本文档按模块说明 `everyday_plan` 项目中主要源文件的功能与简单代码结构。项目基于 Qt 6、QML、C++ 与 SQLite，实现待办任务、今日任务、分类、提醒、甘特图、登录、设置和备份等功能。

> 说明：本文只介绍项目自身源文件，不介绍 `deploy/` 目录下 Qt 部署产生的运行库文件。

## 1. 项目入口与构建

### `main.cpp`

程序入口文件，负责创建 Qt 应用、初始化数据库、注册 C++ 类型到 QML，并加载主界面。

主要流程：

```cpp
DatabaseManager::instance()->initialize("./data.db");
qmlRegisterSingletonInstance<DatabaseManager>("DatabaseManager", 1, 0, "DatabaseManager", DatabaseManager::instance());
engine.rootContext()->setContextProperty("AbstractContentsModel", abstractContentsModel);
engine.loadFromModule("everyday_plan", "Main");
```

它把 `DatabaseManager`、`AuthManager`、`OneDriveManager`、`NotificationManager` 注册为 QML 单例，并把 `AbstractContentsModel`、`GanttModel` 放入 QML 上下文。

### `CMakeLists.txt`

Qt/CMake 构建配置文件。主要作用：

- 设置 C++17。
- 查找 Qt6 模块：`Quick`、`QuickControls2`、`Sql`、`Network`、`Widgets`。
- 创建可执行程序 `appeveryday_plan`。
- 注册 QML 模块 `everyday_plan`。
- 将 QML、C++ 源文件和图标资源加入构建。

### `windows.rc`

Windows 资源文件，用于设置应用图标等资源。

### `everyday_plan_installer.iss`

Inno Setup 安装包脚本，用于生成 Windows 安装程序。

---

## 2. 数据库与业务层

### `databasemanager.h` / `databasemanager.cpp`

数据库核心类，单例模式，暴露给 QML 使用。

主要功能：

- 初始化 SQLite 数据库。
- 创建和迁移表结构。
- 管理用户、任务、分类、设置和验证码。
- 创建、更新、删除任务。
- 加入/取消今日任务。
- 查询提醒任务。
- 导出和导入数据库备份。

常用接口：

```cpp
Q_INVOKABLE int createTask(...);
Q_INVOKABLE bool updateTask(int taskId, const QVariantMap &data);
Q_INVOKABLE bool markTaskForToday(int taskId);
Q_INVOKABLE bool clearTaskFromToday(int taskId);
Q_INVOKABLE bool deleteTask(int taskId);
Q_INVOKABLE QVariantList getDueReminderTasks(int userId, const QString &fromDateTime, const QString &toDateTime);
```

核心数据表包括：`Users`、`UserSettings`、`Categories`、`Tasks`、`VerificationCodes`。

### `init_database.sql`

数据库初始化 SQL 参考文件，描述用户、任务、分类等表结构。实际运行时主要由 `DatabaseManager::createTables()` 创建表。

---

## 3. 任务列表模型

### `abstractContentsItem.h` / `abstractContentsItem.cpp`

表示单条任务数据的 QObject 类。

主要属性：

- `index_num`：任务 ID。
- `title`：标题。
- `content`：正文。
- `outline`：概要。
- `time`：提醒时间。
- `startDate` / `dueDate`：开始和结束时间。
- `priority`：优先级。
- `categoryName` / `categoryColor`：分类显示信息。
- `completed`：是否完成。
- `todaySelected`：是否在今日任务中。

这些属性通过 `Q_PROPERTY` 暴露给 Qt Model 和 QML。

### `abstractcontentsmodel.h` / `abstractcontentsmodel.cpp`

中间任务列表使用的 `QAbstractListModel`。

主要功能：

- 从数据库加载任务。
- 支持今日任务、已完成任务过滤。
- 支持按优先级、创建时间、开始时间、结束时间排序。
- 将任务字段映射成 QML role，例如 `title`、`outline`、`priority`、`todaySelected`。

核心接口：

```cpp
Q_INVOKABLE bool loadAllFromDatabase(const QString &dbPath, int userId, bool todayOnly, bool completedOnly, const QString &sortField, bool sortDescending);
Q_INVOKABLE QVariantMap get(int row) const;
```

QML 中通过 `model.title`、`model.outline`、`model.completed` 等访问数据。

---

## 4. 用户、通知与云同步

### `authmanager.h` / `authmanager.cpp`

用户认证管理器，负责邮箱验证码登录。

主要功能：

- 请求验证码。
- 发送验证码邮件。
- 使用验证码登录。
- 保存和恢复登录状态。
- 更新用户昵称。
- 登出。

常用 QML 属性：

```cpp
isLoggedIn
currentUserId
currentUserEmail
currentUserNickname
```

### `notificationmanager.h` / `notificationmanager.cpp`

系统通知和托盘管理器。

主要功能：

- 显示普通通知。
- 显示提醒通知。
- 最小化到托盘。
- 托盘菜单恢复窗口或退出程序。
- 播放系统提示音。

### `oneDriveManager.h` / `oneDriveManager.cpp`

OneDrive 管理器。

主要功能：

- 设备码登录 OneDrive。
- 保存和刷新 token。
- 上传备份文件。
- 打开验证页面。
- 断开 OneDrive 账户。

### `oneDriveAppConfig.h` / `oneDriveAppConfig.cpp`

OneDrive 应用配置文件，通常保存内置 client id 或相关常量。

---

## 5. 甘特图模型

### `ganttmodel.h` / `ganttmodel.cpp`

甘特图数据模型，继承 `QAbstractListModel`。

主要功能：

- 根据用户 ID 加载任务。
- 管理甘特图显示日期范围。
- 计算任务条的开始偏移、持续时间和宽度。
- 更新任务开始/结束日期。
- 更新任务进度。

核心接口：

```cpp
Q_INVOKABLE void loadTasks();
Q_INVOKABLE void moveToPreviousWeek();
Q_INVOKABLE void moveToNextWeek();
Q_INVOKABLE void moveToToday();
Q_INVOKABLE bool updateTaskDates(int taskId, const QDate &startDate, const QDate &endDate);
```

### `GanttTaskItem`

定义在 `ganttmodel.h` 中，表示甘特图中的单条任务，包含标题、开始日期、结束日期、进度、优先级、分类颜色等信息。

---

## 6. 主窗口与布局 QML

### `Main.qml`

应用主窗口和全局状态中心。

主要职责：

- 管理当前页面：今日、全部任务、已完成、甘特图、设置。
- 管理右侧面板：空状态、任务详情、新建任务、新建分类、设置。
- 打开、保存、删除任务。
- 管理任务标题、概要、正文、附件、时间、提醒、优先级、分类等编辑状态。
- 处理登录状态变化。
- 轮询提醒并调用通知。
- 处理备份和恢复。

重要函数：

```qml
showTodayTasks()
showAllTasks()
showCompletedTasks()
showGanttChart()
openTaskDetail(...)
saveSelectedTaskEdits()
refreshCurrentView()
checkDueReminders()
```

### `MainSidebar.qml`

左侧导航栏。

功能包括：

- 显示今日任务、全部任务、已完成、甘特图、设置入口。
- 显示搜索框。
- 显示分类入口。
- 提供备份快捷按钮。

### `MainNavToolButton.qml`

侧边栏按钮组件，封装统一样式、选中态和点击行为。

### `MainMiddlePanel.qml`

中间列表区域。

主要功能：

- 显示当前页面标题。
- 显示排序菜单。
- 承载 `AbstractContents.qml`。
- 将任务选择事件转发给 `Main.qml`。

### `MainRightShell.qml`

右侧内容外壳，提供统一背景、边框和右侧区域承载能力。

---

## 7. 任务列表 QML

### `AbstractContents.qml`

任务列表容器。

主要功能：

- 使用 `ListView` 展示 `AbstractContentsModel`。
- 按搜索关键词过滤任务。
- 按分类过滤任务。
- 处理任务点击打开详情。
- 处理列表内概要编辑。
- 处理完成、今日、分类变更等操作。

### `ContensElement.qml`

单个任务卡片组件。

显示内容：

- 标题。
- 概要编辑框。
- 分类颜色点。
- 完成按钮。
- 今日按钮。
- 分类下拉框。
- 提醒时间。
- 优先级状态。

主要信号：

```qml
markTodayRequested(bool selected)
completedToggled(bool completed)
detailedRequested()
outlineEdited(string outline)
categoryChanged(int categoryId, string categoryName, string categoryColor)
```

---

## 8. 任务详情 QML

### `DetailHeaderBar.qml`

任务详情顶部标题栏。

功能：

- 显示任务标题。
- 点击标题进入编辑模式。
- 编辑完成后触发保存。
- 提供删除按钮。

### `DetailMetaSection.qml`

任务详情元信息区域。

功能：

- 显示作者、创建时间。
- 编辑提醒时间。
- 编辑开始时间和结束时间。
- 编辑优先级。
- 编辑分类。

主要信号：

```qml
reminderEdited(string value)
reminderEnabledEdited(bool value)
startDateEdited(string value)
dueDateEdited(string value)
priorityEdited(int value)
categoryIndexEdited(int value)
```

### `DetailEditingSection.qml`

任务详情主体编辑区。

功能：

- 编辑概要。
- 编辑正文清单。
- 解析和组合附件路径。
- 上传、替换、删除附件。
- 打开本地附件。
- 切换任务完成状态。

关键辅助函数：

```qml
attachmentPathsFromContent(content)
bodyTextFromContent(content)
composeContent(bodyText, attachmentPaths)
checklistItemsFromContent(content)
checklistContentFromItems(items)
```

### `EmptyDetailState.qml`

右侧未选择任务时的空状态提示组件。

---

## 9. 新建任务与分类 QML

### `NewTaskDialog.qml`

新建任务页面。

功能：

- 输入标题、概要、正文。
- 设置开始/结束时间。
- 选择优先级。
- 选择分类。
- 设置完成状态。
- 选择附件。
- 通过 `submitRequested(...)` 把表单提交给 `Main.qml`。

### `NewCategoryDialog.qml`

新建或编辑分类页面，负责输入分类名称和颜色。

### `CategoryListPanel.qml`

分类列表面板，负责显示、选择、编辑和删除分类。

---

## 10. 登录与设置 QML

### `LoginWindow.qml`

登录窗口。

功能：

- 输入邮箱。
- 请求验证码。
- 输入验证码。
- 调用 `AuthManager.loginWithCode()` 登录。
- 登录成功后通知主窗口。

### `SettingsPanel.qml`

设置页面。

功能：

- 修改主题、背景图、字体大小。
- 修改时间显示格式。
- 控制详情页字段显示。
- 控制甘特图主题。
- 设置备份目录。
- 执行备份和恢复。
- 修改用户显示名。
- 退出登录。
- 测试系统通知。

---

## 11. 甘特图 QML

### `GanttChart.qml`

甘特图主视图。

功能：

- 左侧显示任务名称列表。
- 右侧显示日期网格和任务条。
- 显示优先级标签 `P1` 到 `P4`。
- 提供上一周、下一周、今天等操作。

### `GanttHeader.qml`

甘特图日期头部，显示日期列并突出今天。

### `GanttTaskBar.qml`

甘特图任务条组件，根据任务开始日期、持续时间和进度绘制任务条。

---

## 12. 其他辅助文件

### `create-design-doc.js`

Node.js 文档生成脚本，用于生成设计说明或 Word 文档内容。

### `_patch_enter_next_checklist.py`

开发辅助脚本，从文件名看用于修补清单回车创建下一步的逻辑。

### `.gitignore`

Git 忽略规则文件，用于忽略构建产物、临时文件、数据库文件等。

---

## 13. 主要调用关系

### 任务列表加载

```text
Main.qml
  -> AbstractContentsModel.loadAllFromDatabase(...)
    -> abstractcontentsmodel.cpp 查询 SQLite
      -> AbstractContents.qml / ContensElement.qml 展示
```

### 任务保存

```text
详情 QML 组件
  -> Main.qml 更新 editTaskXXX 状态
    -> saveSelectedTaskEdits()
      -> DatabaseManager.updateTask(...)
        -> SQLite Tasks 表
```

### 今日任务

```text
ContensElement.qml 点击今日
  -> AbstractContents.qml markTaskForToday(...)
    -> DatabaseManager.markTaskForToday(...) 或 clearTaskFromToday(...)
    -> refreshCurrentView()
```

### 提醒通知

```text
Main.qml reminderPollTimer
  -> checkDueReminders()
    -> DatabaseManager.getDueReminderTasks(...)
    -> NotificationManager.showReminderNotification(...)
```

### 甘特图

```text
Main.qml showGanttChart()
  -> GanttModel.loadTasks()
    -> GanttChart.qml / GanttTaskBar.qml 展示
```

---

## 14. 任务表字段简述

| 字段 | 说明 |
| --- | --- |
| `task_id` | 任务 ID |
| `user_id` | 用户 ID |
| `title` | 标题 |
| `description` | 概要 |
| `content` | 正文或附件内容 |
| `start_date` | 开始时间 |
| `end_date` | 结束时间 |
| `today_until` | 今日任务有效期 |
| `today_hidden_until` | 今日任务隐藏期限 |
| `reminder_at` | 提醒时间 |
| `progress` | 进度 |
| `priority` | 优先级，1 到 4 |
| `status` | 完成状态 |
| `category_id` | 分类 ID |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |

---

## 15. 优先级说明

| 数字 | 显示 | 含义 |
| --- | --- | --- |
| `1` | `P1` | 低优先级 |
| `2` | `P2` | 中优先级 |
| `3` | `P3` | 高优先级 |
| `4` | `P4` | 紧急或最高优先级 |

---

## 16. 维护建议

1. 新增任务字段时，需要同步修改数据库、`DatabaseManager`、`AbstractContentsItem`、`AbstractContentsModel` 和相关 QML。
2. QML 文件使用 C++ 单例时，应在该 QML 文件中显式 import 对应模块。
3. 任务保存建议统一走 `DatabaseManager.updateTask()`。
4. 修改列表显示字段后，要检查 `roleNames()` 和 SQL 查询是否同步。
5. `deploy/` 目录通常是部署产物，不建议手动修改。
